export function buildComposition(scene) {
  // We can dynamically adjust this based on scene or input, 
  // but following the Instagram rules:
  return {
    framing: "medium shot, cinematic",
    subject_position: "off-center",
    negative_space: "generous negative space reserved for text overlay",
    style: "editorial cinematic, premium instagram aesthetic",
    imperfection: "natural, subtle grain, organic"
  };
}
